parameters:
  agentPool: ''
  env: ''
  deploySynapse: true
  activateTriggers: false
  runUnitTests: true
  runIntegrationTests: True
  runEndToEndTests: true

##
# Deploy Azure Synapse
##
jobs:
- job: Deploy_Synapse
  pool: ${{ parameters.agentPool }}
  condition: eq(parameters.deploySynapse, true)
  steps:
  - checkout: self
    clean: true
  - task: AzureCLI@2
    displayName: 'Testing'
    inputs:
      azureSubscription: 'ODW ${{ upper(parameters.env) }} - Infrastructure'
      scriptType: 'bash'
      scriptLocation: 'inlineScript'
      inlineScript: |
        echo "testing"